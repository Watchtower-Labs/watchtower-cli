# Watchtower Web Dashboard — local environment variables
# Copy this file to .env.local and adjust as needed.

# Directory where trace JSONL files are stored.
# Defaults to ~/.watchtower/traces if not set.
# WATCHTOWER_TRACE_DIR=/path/to/your/traces
